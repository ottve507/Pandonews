var $j = jQuery.noConflict();
	$j('#feedboxes').append('<%= j render :partial => 'feeds', :collection => @feeds %>');
	<% if @feeds.next_page %>
  	$j('.pagination').replaceWith('<%= j will_paginate(@feeds) %>');
	<% else %>
  	$j('.pagination').remove();
	<% end %>
<% sleep 1 %>
